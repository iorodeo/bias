#include "stampede_plugin.hpp"

namespace bias
{

    // Public Methods
    // ------------------------------------------------------------------------
    StampedePlugin::StampedePlugin(QWidget *parent) : QDialog(parent)
    {
        setupUi(this);
    }

}
